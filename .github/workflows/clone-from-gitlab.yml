name: Clone from Gitlab



on:
  workflow_dispatch:
    inputs:
      branch:
        default: master
jobs:
  clone:
    name: Clone repo
    runs-on: ubuntu-latest
    steps:
      - name: "Clone from Gitlab"
        run: | 
          git clone https://gitlab.c3sl.ufpr.br/mmcavazotti/the-algorithm-box.git
          cd the-algorithm-box
      - name: "Checkout branch"
        run: | 
          git checkout ${{ inputs.branch}}
      - name: "Push to Github"
        run: | 
          git add remote github https://github.com/mcavazotti/the-algorithm-box.git
          git push github
